<h2>ユーザーの詳細ページ</h2>
<p>Name: <%= @user.name%></p>
<p>Email: <%= @user.email%></p>

<% @user.questions.each do |question| %>
  <%= link_to(question.title, "/questions/#{question.id}") %>
<% end %>